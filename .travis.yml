cache: bundler

language: ruby

rvm:
  - 2.3.8
  - 2.4.5
  - 2.5.3

sudo: false

branches:
  only: master

before_install:
  - gem update --system
  - gem install bundler

env:
  global:
    - ZUORA_CLIENT_ID=something
    - ZUORA_CLIENT_SECRET=secret
    - ZUORA_DOMAIN=rest.apisandbox.zuora.com
